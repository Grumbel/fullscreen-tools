env = Environment()
env.Append(CXXFLAGS=[ "-g", "-O0", "-ansi", "-pedantic", "-Wall", "-Weffc++", "-Wconversion", "-Werror" ])
env.Append(LIBS="X11")

env.Program("windowhierachy", ["windowhierachy.cpp"])
env.Program("capturewindow", ["capturewindow.cpp"])

# EOF #
